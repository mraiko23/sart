{
  "name": "puter-api-bridge",
  "version": "1.0.0",
  "main": "server.js",
  "license": "MIT",
  "scripts": {
    "start": "node server.js",
    "build": "yarn install && node node_modules/puppeteer/install.mjs"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^21.11.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
